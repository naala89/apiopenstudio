class_name: ApiTester
modules:
    enabled:
        - \Helper\Api
        - REST:
            url: "https://%API_DOMAIN%"
            depends: PhpBrowser
        - Filesystem
env:
    local:
        modules:
            config:
                REST:
                    url: "https://%API_DOMAIN%"
    ci:
        modules:
            config:
                REST:
                    url: "http://%LOCAL_IP%"
    staging:
        modules:
            config:
                REST:
                    url: "https://%API_DOMAIN%"
    production:
        modules:
            config:
                REST:
                    url: "https://%API_DOMAIN%"