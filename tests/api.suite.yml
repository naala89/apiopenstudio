class_name: ApiTester
modules:
    enabled:
        - \Helper\Api
        - REST:
            url: https://api.gaterdata.local
            depends: PhpBrowser
        - Filesystem
env:
    local:
        modules:
            config:
                PhpBrowser:
                    url: https://admin.gaterdata.local
                REST:
                    url: https://api.gaterdata.local
    staging:
        modules:
            config:
                PhpBrowser:
                    url: https://admin.gaterdata.local
                REST:
                    url: https://api.gaterdata.local
    production:
        modules:
            config:
                PhpBrowser:
                    url: https://admin.gaterdata.local
                REST:
                    url: https://api.gaterdata.local