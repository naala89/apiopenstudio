name: resource application no developer role

description: test create resource with no developer role for application

uri: processors

method: get

appid: 1

ttl: 0

security:
  processor: token_role
  id: resource application no developer role security
  token:
    processor: var_get
    id: resource application no developer role token
    key: token
  role: Consumer

process:
  processor: processors
  id: resource application no developer role process
  machine_name: all
