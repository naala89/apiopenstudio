name: resource no uri

description: test new resource with missing uri

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: test bad resource no uri security
  token:
    function: var_get
    id: test bad resource no uri token
    key: token
  role: Consumer

process:
  function: functions
  id: test bad resource no uri process
  machine_name: all

output:
  - response
  - function: email
    to: john@naala.com.au
    subject: "test email"
    format: html
  - function: xml
    destination: www.foo.bar
    method: post
