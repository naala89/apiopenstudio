name: resource output response

description: test resource with output response only

uri: resource/output/response

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: test resource with output response only security
  token:
    function: var_get
    id: test resource with output response only token
    key: token
  role: Consumer

process:
  function: functions
  id: test resource with output response only process
  machine_name: all

output:
  - response

