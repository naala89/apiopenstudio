name: test resource with bad min

description: test resource with bad min

uri: resource/badmin

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: test resource with bad min security
  token:
    function: var_get
    id: test resource with bad min token
    key: token
  role: Consumer

process:
  function: concatenate
  id: test resource with bad min process
  sources:
    - zsdkfjh

output:
  - function: email
    id: test resource with bad min email
    to: john@naala.com.au
    subject: "test email"
    format: html
  - function: xml
    id: test resource with bad min xml
    destination:
      - www.foo.bar
      - www.foo.bar
      - www.foo.bar
    method: post
