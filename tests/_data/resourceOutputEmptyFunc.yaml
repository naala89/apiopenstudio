name: resource output empty func

description: test resource with empty function as output

uri: resource/output/empty

method: get

appid: 2

ttl: 0

security:
  processor: token_role
  id: test resource with empty function as output security
  token:
    processor: var_get
    id: test resource with empty function as output token
    key: token
  role: Consumer

process:
  processor: functions
  id: test resource with empty function as output process
  machine_name: all

output:
  - function:
    id: test resource with empty function as output output 0
    to: john@naala.com.au
    subject: "test email"
    format: html
  - processor: xml
    id: test resource with empty function as output output 1
    destination: www.foo.bar
    method: post

