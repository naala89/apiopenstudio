uri: resource/missing/name

description: Test resource with no name

method: get

appid: 2

ttl: 0

security:
  processor: validate_token_roles
  id: test resource with no name security
  roles:
    - Consumer

process:
  processor: processors
  id: test resource with no name process
  machine_name: all
