name: test resource w missing method

description: test resource with missing method

uri: resource/missing/method

appid: 2

ttl: 0

security:
  processor: token_role
  id: test bad resource no method security
  token:
    processor: var_get
    id: test bad resource no method token
    key: token
  role: Consumer

process:
  processor: processors
  id: test bad resource no method process
  machine_name: all

output:
  - response
  - processor: email
    to: john@naala.com.au
    subject: "test email"
    format: html
  - processor: xml
    destination: www.foo.bar
    method: post

