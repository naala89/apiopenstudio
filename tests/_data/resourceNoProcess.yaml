name: resource no process

description: test new resource with missing process

uri: resource/missing/process

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: test bad resource no process security
  token:
    function: var_get
    id: test bad resource no process token
    key: token
  role: Consumer

output:
  - response
  - function: email
    to: john@naala.com.au
    subject: "test email"
    format: html
    message: "test email"
  - function: xml
    destination: www.foo.bar
    method: post
