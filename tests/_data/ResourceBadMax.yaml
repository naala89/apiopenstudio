name: test resource with bad max

description: test resource with bad max

uri: resource/badmax

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: test resource with bad max security
  token:
    function: var_get
    id: test resource with bad max token
    key: token
  role: Consumer

process:
  function: var_field
  id: test resource with bad max process
  key: zsdkfjh
  value:
    - zsdkfjh
    - dfhstfn

output:
  - function: email
    id: test resource with bad max email
    to: john@naala.com.au
    subject: "test email"
    format: html
  - function: xml
    id: test resource with bad max xml
    destination:
      - www.foo.bar
      - www.foo.bar
      - www.foo.bar
    method: post
