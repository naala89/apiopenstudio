name: Test var_field

description: Test creating a var_field from vars and constants.

uri: field

method: get

appid: 2

ttl: 0

security:
  processor: token_role
  id: test var_field security
  token:
    processor: var_get
    id: test var_field token
    key: token
  role: Consumer

process:
  processor: var_field
  id: test var_field process
  key: my_test_var
  value:
    processor: var_loosely_typed
    id: test var_field value
    value: my_test_val