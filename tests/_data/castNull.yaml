name: Testing cast on a empty null

description: Test cast processor with empty null

uri: cast/empty

method: get

appid: 2

ttl: 0

security:
    processor: validate_token_roles
    id: test cast empty security
    roles:
        - Developer

process:
    processor: cast
    id: test cast empty cast
    data:
        processor: var_literal
        id: test cast empty data
        value: null
        type: empty
    data_type:
        processor: var_get
        id: test cast empty data_type
        key: data_type
        expected_type: text
