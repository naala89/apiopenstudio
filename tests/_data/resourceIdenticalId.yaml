name: test resource identical IDs

description: test resource identical IDs

uri: resource/identical/ids

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: resource identical ids security
  token:
    function: var_get
    id: resource identical ids token
    key: token
  role: Consumer

process:
  function: functions
  id: resource identical ids

output:
  - function: email
    id: resource identical ids
    to: john@naala.com.au
    subject: "test email"
    format: html
    message: test message
  - function: xml
    id: resource identical ids xml
    destination: www.foo.bar
    method: post
