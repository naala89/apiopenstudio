name: resource no ttl

description: test new resource with missing ttl

uri: resource/missing/ttl

appid: 2

method: get

security:
  function: token_role
  id: test bad resource no ttl security
  token:
    function: var_get
    id: test bad resource no ttl token
    key: token
  role: Consumer

process:
  function: functions
  id: test bad resource no ttl process
  machine_name: all

output:
  - response
  - function: email
    to: john@naala.com.au
    subject: "test email"
    format: html
  - function: xml
    destination: www.foo.bar
    method: post

