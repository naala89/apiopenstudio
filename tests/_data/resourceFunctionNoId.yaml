name: test resource w no id

description: test resource with no id

uri: resource/function/noid

method: get

appid: 2

ttl: 0

security:
  function: token_role
  id: test bad resource with function without id security
  token:
    function: var_get
    id: test bad resource with function without id token
    key: token
  role: Consumer

process:
  function: functions
  id: test bad resource with function without id process
  machine_name: all

output:
  - response
  - function: email
    to: john@naala.com.au
    subject: "test email"
    format: html
    message: "test email"
  - function: xml
    destination: www.foo.bar
    method: post
