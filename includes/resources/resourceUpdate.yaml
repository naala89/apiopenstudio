name: Resource update

description: update a resource.

uri: resource

method: put

appid: 2

ttl: 0

security:
  function: token_role
  id: resource_update_security
  token:
    function: bearer_token
    id: resource_update_token
  role: Developer

process:
  function: resource_update
  id: resource_update_process
  body:
    function: var_body
    id: resource_update_body
    nullable: false
