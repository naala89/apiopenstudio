uri:
  noun: processors
  verb: all

method: get

ttl: 3600

security:
  processor: tokenDeveloper
  meta:
    id: 1
    token:
      processor: varGet
      meta:
        id: 2
        name: token

process:
  processor: ProcessorsAll
  meta:
    id: 3

output:
  - response
  - email:
      to: john@naala.com.au
      subject: "test email"
      format: html

