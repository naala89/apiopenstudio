name: JSON import

description: Create a resource from a document or string in JSON format.

uri:
  noun: resource
  verb: json

method: post

ttl: 0

security:
  processor: tokenDeveloper
  meta:
    id: 1
    token:
      processor: varPost
      meta:
        id: 2
        name: token

process:
  processor: resourceJson
  meta:
    id: 3
    yaml:
      processor: varPost
      meta:
        id: 4
        name: yaml

