name: User delete

description: Delete a user.

application: Datagator

uri:
  noun: user
  verb: manage

method: delete

ttl: 0

security:
  processor: TokenSysAdmin
  meta:
    id: 1
    token:
      processor: varGet
      meta:
        id: 2
        name: token

process:
  processor: datagatorUser
  meta:
    id: 3
    username:
      processor: varGet
      meta:
        id: 4
        name: username
