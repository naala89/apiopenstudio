name: Resource import YAML

description: Create a resource from a document or string in YAML format.

uri: resource/yaml

method: post

ttl: 0

security:
  function: tokenDeveloper
  id: 1
  token:
    function: varPost
    id: 2
    name: token

process:
  function: resourceYaml
  id: 3
  resource: resource
