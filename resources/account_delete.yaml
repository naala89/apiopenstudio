name: Account delete

description: Delete an account if any applications do not belong to the account.

uri: account

method: delete

appid: 2

ttl: 0

security:
  function: token_role
  id: account_delete_security
  token:
    function: bearer_token
    id: account_delete_bearer_token
  role: Administrator

process:
  function: account_delete
  id: account_delete_process
  accid:
    function: var_uri
    id: account_delete_accid
    index: 0
