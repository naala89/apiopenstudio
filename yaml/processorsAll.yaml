uri:
  noun: processors
  verb: all

method: get

application: All

ttl: 3600

security:
  processor: tokenDeveloper
  meta:
    id: 1
    token:
      processor: varGet
      meta:
        id: 2
        name: token

process:
  processor: ProcessorsAll
  meta:
    id: 3
