name: Processors all

description: Fetch details of all procssors.

uri:
  noun: processors
  verb: all

method: get

ttl: 3600

security:
  processor: tokenDeveloper
  meta:
    id: 1
    token:
      processor: varGet
      meta:
        id: 2
        name: token

process:
  processor: ProcessorsAll
  meta:
    id: 3
