uri:
  noun: broken
  verb: yaml

method: get

ttl: 0

process:
  processor: object
  meta:
    id: 1
    attributes:
      - processor: url
        meta:
          id: 2
          method: get
          source: "http://rest.db.ripe.net/ripe/inetnum/34.54.212.4.json"
          reportError: 3
          normalise:
      - processor: url
        meta:
          id: 3
          method: get
          source: "http://www.xmlfiles.com/examples/cd_catalog.xml"
          reportError: 1
          normalise: 1
      - processor: field
        meta:
          id: 4
          key:
          value: value1

Description: test broken Resource

DataType:

  Processor:

    Name:
      Type: String
      Description: Name of the Processor

    Meta: {
      Name: String,
      Value: String::Processor
    }

Structure:

  UserId:
    Type: Integer
    Description: User ID Number

  Process:

    ProcessType:
      Type: String
      Description: Name of the Processor

    ProcessorMeta:
      Type: String::Processor